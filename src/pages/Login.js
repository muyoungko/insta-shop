import React from 'react';

const Login = ({match}) => {
    return (
        <div>
            <h2>
                Main
            </h2>
            Login
        </div>
    );
};

export default Login;
