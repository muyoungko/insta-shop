import React from 'react';

const Order = ({match}) => {
    return (
        <div>
            <h2>
                Order
            </h2>
            주문완료
        </div>
    );
};

export default Order;
